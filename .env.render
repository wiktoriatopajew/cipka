# Render.com Environment Variables Template
# Copy these to your Render Web Service environment variables

# Required - Basic Setup
NODE_ENV=production
PORT=10000

# Required - Database (get from your PostgreSQL service on Render)
DATABASE_URL=postgresql://username:password@host:port/database

# Required - Security
SESSION_SECRET=your-super-secret-session-key-change-this

# Optional - Payments (for full functionality)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Optional - Email (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# Optional - Admin (will be created automatically if not set)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=secure-admin-password